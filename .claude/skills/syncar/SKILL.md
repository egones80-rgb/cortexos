---
name: syncar
description: Alias do /salvar. Mesmo comportamento: commit + push pro GitHub. Use quando o usuário disser "syncar", "synca", "sincronizar".
---

# /syncar

Atalho. Mesmo que `/salvar` — faz commit e push pro remote configurado.

Para o fluxo completo, consulte `.claude/skills/salvar/SKILL.md`.

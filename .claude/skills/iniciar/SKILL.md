---
name: iniciar
description: Alias do /abrir. Mesmo comportamento: carrega memória e devolve resumo de 5 linhas. Use quando o usuário disser "iniciar", "começar", "vamos começar".
---

# /iniciar

Atalho. Mesmo que `/abrir` — carrega `_memoria/` + `identidade/` e devolve resumo curto.

Para o fluxo completo, consulte `.claude/skills/abrir/SKILL.md`.
